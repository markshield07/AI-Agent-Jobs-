# Anthropic API key used for resume parsing, job scoring and tailoring.
ANTHROPIC_API_KEY=sk-ant-...

# Where the SQLite file and uploaded resumes live. Defaults to ./data
JOBAGENT_DATA_DIR=./data

# Model used for parsing and tailoring.
JOBAGENT_MODEL=claude-opus-5
